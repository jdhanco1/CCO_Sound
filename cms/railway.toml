[build]
  builder        = "DOCKERFILE"
  dockerfilePath = "Dockerfile"
  dockerBuildTarget = "prod"

[deploy]
  # Railway injects $PORT; Next.js reads it via CMD below
  healthcheckPath    = "/"
  healthcheckTimeout = 300
  restartPolicyType  = "ON_FAILURE"
  restartPolicyMaxRetries = 3
